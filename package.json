{
  "name": "zzapi-mes",
  "private": true,
  "version": "0.1.0",
  "description": "SAP ICF REST handler SDK + CLI for MES integration",
  "license": "UNLICENSED",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "smoke": "bash scripts/smoke.sh",
    "spec:gen": "bash scripts/spec-gen.sh"
  },
  "devDependencies": {
    "openapi-zod-client": "^1.18.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["argon2", "better-sqlite3", "esbuild"]
  },
  "packageManager": "pnpm@10.17.1"
}
