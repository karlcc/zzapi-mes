# zzapi-mes hub environment variables
# Copy to /etc/zzapi-mes-hub.env and fill in values.

# Hub config
HUB_PORT=8080
HUB_JWT_SECRET=change-me-to-a-random-string
HUB_API_KEYS=key1,key2
# HUB_LOG_FILE=/var/log/zzapi-mes-hub.log

# SAP backend (the hub holds these so clients don't need them)
SAP_HOST=sapdev.fastcell.hk:8000
SAP_CLIENT=200
SAP_USER=api_user2
SAP_PASS=Pt@2026
